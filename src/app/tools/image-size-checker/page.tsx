import ImageSizeCheckerClient from './ImageSizeCheckerClient'

export const metadata = {
  title: 'Image Size Checker — OpticShift Pro',
  description: 'Check image dimensions, file size, aspect ratio and social media compatibility instantly.',
}

export default function ImageSizeCheckerPage() {
  return <ImageSizeCheckerClient />
}
