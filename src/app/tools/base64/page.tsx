import Base64Client from './Base64Client'

export const metadata = {
  title: 'Base64 Encoder/Decoder — OpticShift Pro',
  description: 'Encode or decode Base64 strings instantly in your browser. Free and private.',
}

export default function Base64Page() {
  return <Base64Client />
}
